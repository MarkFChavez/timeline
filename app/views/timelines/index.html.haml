%section.cd-container#cd-timeline
  - timelines.each do |timeline|
    .cd-timeline-block
      .cd-timeline-img{ class: "#{check_state(timeline.date)}" }
        = image_tag("cd-icon-location.svg")

      .cd-timeline-content
        %h2 #{timeline.name}

        %span.cd-date #{timeline.render_date}
